# Minimal worker for local CLI/API smoke tests (register before saga.minimal.yaml).
kind: worker
name: minimal-worker
namespace: default
version: "1.0.0"
provider: local
model_name: llama3.2
system_prompt: |
  You are a governed workflow agent running inside Warden.
  Complete the assigned task using the structured output format required by the step.
