# Minimal one-step saga for local CLI/API smoke tests.
# Requires: worker.minimal.yaml, step.minimal-step1.yaml
kind: saga
name: minimal-saga
namespace: default
version: 0.0.1
description: One reason step; for deploy / list / start experiments only.
steps:
  - id: step1
    use: minimal-step1
    version: "0.0.1"
    with: {}
