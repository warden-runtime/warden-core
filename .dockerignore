# Git
.git
.gitignore

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Docker
Dockerfile*
docker-compose.yml

# Example local data (bind-mounted at runtime; must not be in build context)
**/workspace/

# db/migrations is bind-mounted into the compose `migrate` service (not baked into images).

# IDE / OS
.vscode/
.idea/
*.DS_Store